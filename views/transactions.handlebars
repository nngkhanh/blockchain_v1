<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giao diện Smart Contract</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            margin: 0;
        }

        .contain-transactions {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .wallet-info {
            text-align: start;
            margin-bottom: 30px;
            background: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .wallet-info h2 {
            margin: 5px 0;
            opacity: 0.8;
        }

        .actions-container {
            display: flex;
            justify-content: space-between;
            gap: 20px;
            margin-bottom: 30px;
        }

        .action-box {
            flex: 1;
            background: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .action-box h3 {
            margin-bottom: 15px;
        }

        .action-box input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
        }

        .action-box button {
            width: 100%;
            padding: 10px;
            background: #28a745;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        .action-box button:hover {
            background: #218838;
        }

        .transfer-box {
            background: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 0 auto;
        }

        .transfer-box h3 {
            margin-bottom: 15px;
        }

        .transfer-box input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
        }

        .transfer-box button {
            width: 100%;
            padding: 10px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        .transfer-box button:hover {
            background: #0069d9;
        }
    </style>
</head>

<body>
    <div class="contain-transactions">
        <div class="wallet-info">
            <h2>Xin chào: <span>{{userName}}</span></h2>
            <h2>Địa chỉ ví: <span>{{walletAddress}}</span></h2>
            <h2>Số dư ví: <span>{{nativeBalance}} ETH</span></h2>
            <h2>Số dư trong Contract: <span id="contractBalance">{{contractBalance}} ETH</span></h2>
        </div>

        <div class="actions-container">
            <form action="/contract/deposit" method="POST" class="action-box">
                <h3>Nạp tiền vào Contract</h3>
                <input type="number" name="amount" placeholder="Nhập số ETH muốn nạp" step="any" required />
                <button type="submit">Nạp tiền</button>
            </form>

            <div class="transfer-box">
                <h3>Chuyển tiền giữa các tài khoản</h3>
                <form action="/contract/transfer" method="POST" id="transferForm">
                    <input type="text" name="recipient" id="receiverAddress" placeholder="Địa chỉ người nhận (0x...)"
                        required />
                    <input type="number" name="total_amount" id="transferAmount" placeholder="Số ETH muốn chuyển"
                        step="any" required />
                    <button type="submit">Chuyển tiền</button>
                </form>
            </div>

            <form action="/contract/withdraw" method="POST" class="action-box">
                <h3>Rút tiền khỏi Contract</h3>
                <input type="number" name="amount" placeholder="Nhập số ETH muốn rút" required step="any" />
                <button type="submit">Rút tiền</button>
            </form>
        </div>
    </div>
</body>

</html>