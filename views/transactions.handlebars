<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Giao diện Smart Contract</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f4f4f4;
            padding: 20px;
            margin: 0;
        }

        .contain-transactions {
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .wallet-info {
            text-align: center;
            margin-bottom: 30px;
            background: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .wallet-info h2 {
            margin: 5px 0;
        }

        .actions-container {
            display: flex;
            justify-content: space-between;
            gap: 20px;
            margin-bottom: 30px;
        }

        .action-box {
            flex: 1;
            background: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .action-box h3 {
            margin-bottom: 15px;
        }

        .action-box input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
        }

        .action-box button {
            width: 100%;
            padding: 10px;
            background: #28a745;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        .action-box button:hover {
            background: #218838;
        }

        .transfer-box {
            background: #fff;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            margin: 0 auto;
        }

        .transfer-box h3 {
            margin-bottom: 15px;
        }

        .transfer-box input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
        }

        .transfer-box button {
            width: 100%;
            padding: 10px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        .transfer-box button:hover {
            background: #0069d9;
        }
    </style>
</head>

<body>
    <div class="contain-transactions">
        <div class="wallet-info">
            <h2>xin chào {{userName}}</h2>
            <h2>Địa chỉ ví: <span id="walletAddress">{{walletAddress}}</span></h2>
            <h2>Số dư ví: <span id="walletBalance">{{nativeBalance}} ETH</span></h2>
            <h2>Số dư trong Contract: <span id="contractBalance">{{contractBalance}} ETH</span></h2>
        </div>

        <div class="actions-container">
            <div class="action-box">
                <h3>Nạp tiền vào Contract</h3>
                <input type="number" id="depositAmount" placeholder="Nhập số ETH muốn nạp" />
                <button onclick="deposit()">Nạp tiền</button>
            </div>

            <div class="transfer-box">
                <h3>Chuyển tiền giữa các tài khoản</h3>
                <input type="text" id="receiverAddress" placeholder="Địa chỉ người nhận (0x...)" />
                <input type="number" id="transferAmount" placeholder="Số ETH muốn chuyển" />
                <button onclick="transfer()">Chuyển tiền</button>
            </div>

            <div class="action-box">
                <h3>Rút tiền khỏi Contract</h3>
                <input type="number" id="withdrawAmount" placeholder="Nhập số ETH muốn rút" />
                <button onclick="withdraw()">Rút tiền</button>
            </div>
        </div>
    </div>
</body>

</html>