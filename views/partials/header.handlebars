<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Header Sang Trọng - Ví Blockchain</title>
    <link rel="stylesheet" href="css/header.css" />
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Inter:wght@500;600&display=swap"
        rel="stylesheet">
</head>

<body>
    <header class="main-header">
        <div class="logo">Ví Blockchain</div>

        <nav class="nav-center">
            <a href="/" class="nav-button active">Trang chủ</a>
            <a href="/manage-wallet" class="nav-button">Quản lý ví</a>
            <a href="/transactions" class="nav-button">Giao dịch</a>
            <a href="/history" class="nav-button">Lịch sử giao dịch</a>
            <a href="/settings" class="nav-button">Cài đặt</a>
            <a href="/test" class="nav-button">Test</a>
        </nav>

        <div class="auth-buttons">
            {{#if isLoggedIn}}
            <h1 class="user-name">{{user.username}}</h1>
            {{else}}
            <a href="/user/login">
                <button class="btn login">Đăng nhập</button>
            </a>
            <a href="/user/register">
                <button class="btn register">Đăng ký</button>
            </a>
            {{/if}}
        </div>

        <!-- Nút hamburger cho mobile -->
        <div class="hamburger">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </header>

    <!-- JavaScript cho hamburger menu -->
    <script>
        document.querySelector('.hamburger').addEventListener('click', () => {
            document.querySelector('.nav-center').classList.toggle('active');
        });
    </script>
</body>

</html>