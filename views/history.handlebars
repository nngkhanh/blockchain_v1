<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lịch sử giao dịch</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f6f8;
            color: #333;
            margin: 0;
            padding: 0;
        }

        h2 {
            text-align: center;
            font-size: 24px;
            color: #2c3e50;
            margin-bottom: 10px;
        }

        p {
            font-size: 16px;
            margin: 10px 0;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 15px;
        }

        th,
        td {
            padding: 12px 10px;
            border: 1px solid #e1e4e8;
            text-align: left;
        }

        th {
            background-color: #f0f2f5;
            color: #333;
        }

        tr:nth-child(even) {
            background-color: #fafafa;
        }

        tr:hover {
            background-color: #f1f7ff;
        }

        a {
            color: #007BFF;
            text-decoration: none;
            word-break: break-all;
        }

        a:hover {
            text-decoration: underline;
        }

        .no-data {
            color: #888;
            font-style: italic;
            text-align: center;
            margin-top: 20px;
        }

        .status-success {
            color: green;
            font-weight: bold;
        }

        .status-failed {
            color: red;
            font-weight: bold;
        }

        .type-sent {
            color: red;
            font-weight: bold;
        }

        .type-received {
            color: green;
            font-weight: bold;
        }

        .back-link {
            display: block;
            margin-top: 30px;
            text-align: center;
        }

        .back-link a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #3498db;
            color: white;
            border-radius: 6px;
            transition: background-color 0.3s ease;
        }

        .back-link a:hover {
            background-color: #2980b9;
        }

        @media (max-width: 768px) {
            table {
                font-size: 13px;
            }

            th,
            td {
                padding: 8px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Lịch sử giao dịch của {{truncateAddress address}}</h2>
        <p><strong>Tổng số giao dịch:</strong> {{total/partials}} {{totalTransactions}}</p>

        {{#if history.length}}
        <table>
            <thead>
                <tr>
                    <th>Loại</th>
                    <th>Gửi</th>
                    <th>Nhận</th>
                    <th>Số lượng (ETH)</th>
                    <th>Ngày/Giờ</th>
                    <th>Phí Gas (ETH)</th>
                    <th>Block</th>
                    <th>Tx Hash</th>
                    <th>Trạng thái</th>
                </tr>
            </thead>
            <tbody>
                {{#each history}}
                <tr>
                    <td class="{{#if (eq type 'Sent')}}type-sent{{else}}type-received{{/if}}">{{type}}</td>
                    <td>{{truncateAddress from}}</td>
                    <td>{{truncateAddress to}}</td>
                    <td>{{formatEther amount}}</td>
                    <td>{{formatDate timestamp}}</td>
                    <td>{{formatEther gasFee}}</td>
                    <td>{{blockNumber}}</td>
                    <td><a href="https://etherscan.io/tx/{{hash}}" target="_blank">{{truncateHash hash}}</a></td>
                    <td class="{{#if (eq status 'Thành công')}}status-success{{else}}status-failed{{/if}}">{{status}}
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
        <p class="no-data">Không có giao dịch nào được tìm thấy.</p>
        {{/if}}

        <div class="back-link">
            <a href="/">Quay lại trang chủ</a>
        </div>
    </div>
</body>

</html>